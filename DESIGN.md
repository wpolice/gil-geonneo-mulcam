# 길건너 멀캠 — 게임 기획안

## 1. 컨셉
**"길건너 멀캠"**은 Crossy Road(길건너 친구들) 스타일의 3D 복셀 미니게임이다. "집에서
멀티캠퍼스(회사)까지 출근하기"가 테마이며, AI 에이전트 부트캠프 프로젝트라는 맥락을 살려
코인 대신 **실제 AI 서비스 로고**를 수집 아이템으로 사용한다.

## 2. 기술 스택
- 빌드 툴 없이 `index.html`을 열면 바로 실행되는 순수 HTML/CSS/JS.
- 3D 렌더링은 Three.js (CDN importmap으로 로드, 번들러 불필요).
- 파일은 여러 개 JS 모듈로 분리 (`js/` 폴더 아래).

## 3. 비주얼 & 캐릭터
- Crossy Road와 동일한 3D 복셀(voxel) 스타일, 외부 3D 모델 파일 없이 `THREE.BoxGeometry` 조합으로
  프로시저럴 제작 (플레이어, 자동차, 버스, 전동차 모두 코드로 직접 조립).
- 캐릭터는 본인을 닮은 직장인 사람 (셔츠 + 가방을 멘 미니 복셀 캐릭터).
- 카메라: `OrthographicCamera`로 아이소메트릭 느낌, 플레이어를 부드럽게 따라감.

## 4. 조작
- **방향키(↑ ↓ ← →)만 사용**, 한 번 누르면 그 방향으로 한 칸(grid) 이동.
- WASD·모바일 터치는 이번 버전 범위 밖 (추후 확장 여지만 열어둠).

## 5. 스테이지 구성 (5단계, 일반적인 도시 출퇴근 느낌 — 실제 지명 없음)
| # | 스테이지명 | 주요 장애물 |
|---|---|---|
| 1 | 우리집 (시작) | 장애물 거의 없음, 조작 적응 구간 |
| 2 | 큰길 횡단보도 | 승용차 · 트럭 (중속~고속) |
| 3 | 버스 정류장 | 정차/출발하는 버스 |
| 4 | 지하철 승강장 | 전동차 통과 타이밍 |
| 5 | 회사 앞 광장 (멀티캠퍼스, 도착) | 장애물 없음, 결승선 |

스테이지 사이는 급격히 끊기지 않고 레인이 자연스럽게 이어져 "하나의 이어진 출근길"처럼
느껴지도록 구성. 진행할수록 장애물 속도/밀도가 살짝 증가.

## 6. 게임 종료 조건 (무한 모드 없음)
- **장애물과 충돌 → 즉시 게임오버.** (사망 조건은 이것 하나뿐 — 원작 Crossy Road처럼 "카메라에
  뒤처져서 화면 밖으로 밀려 죽는" 방식은 넣지 않음. 카메라는 그냥 따라가기만 함)
- **마지막 스테이지(회사 앞 광장) 도착 → 성공.** 도착 시각에 따라 등급 판정 후 종료.
- 두 경우 모두 결과 화면에서 "다시 시작" 버튼으로 재시작.

## 7. 출근 시계 & 등급
- 인게임 시계는 08:30부터 시작, 한 칸 이동할 때마다 시간이 조금씩 흐름.
- 너무 오래 멈춰 있으면 약간의 페널티 시간 추가.
- 도착 시 등급 (칼출근·정시출근은 사실상 같은 말이라 중간 등급 이름을 구분되게 변경):
  - 09:00 이전 → **칼출근 🎉** (최고 보너스)
  - 09:00~09:05 → **턱걸이출근 😅** (아슬아슬하게 겨우 도착, 기본 보너스)
  - 09:05 초과 → **지각 💦** (보너스 없음, 게임오버는 아니고 유머러스한 결과 문구)

## 8. 수집 아이템 — AI 로고 & 파워업
- 코인 대신 실제 AI 서비스 로고 **4종(ChatGPT, Claude, Gemini, Copilot)** 을 맵 곳곳에 배치,
  지나가며 수집. 각 로고는 점수 + 각 서비스의 실제 정체성에서 착안한 순간 파워업 효과를 함께 준다
  ("그냥 붙인 효과"가 아니라 그 서비스라서 그 효과라는 근거를 분명히 함).

  | 로고 | 파워업 효과 | 근거 |
  |---|---|---|
  | ChatGPT | 3초 무적 | 만능 올라운더 |
  | Claude | 충돌 1회 무효화 (세이프티 배리어) | AI Safety가 핵심 미션 |
  | Gemini | 출근 시계 일부 되돌리기 | 긴 컨텍스트(기억력)가 특징 |
  | Copilot | 다음 위험 구간 자동 회피 1회 | 자동 완성 페어 프로그래머 |

- **배포 계획이 개인 로컬 실행 전용(비공개)** 이므로 실제 로고 이미지를 그대로 사용해도 실질적
  리스크가 낮음. (단, 추후 GitHub 공개/배포 URL 공유/데모데이 발표 등 공개 배포로 계획이 바뀌면
  스타일화된 버전으로 교체 권장.)
- 로고 이미지 파일은 사용자가 직접 `assets/logos/`에 채워 넣음 (자동 다운로드는 진행하지 않음).

## 9. 점수 & 저장
- 점수 = (전진한 행 수 × 10) + (수집한 로고 개수 × 50) + (도착 등급 보너스: 칼출근 500 / 턱걸이출근 200 / 지각 0)
- **장애물에 부딪혀 게임오버가 되어도 그때까지의 점수(전진 칸 수 + 로고 수집분)는 그대로 결과
  화면에 표시된다.** 다만 도착하지 못했으므로 등급 보너스는 붙지 않는다.
- `localStorage`에 최고 점수 · 최고 등급 · 최단 도착 시각 저장.

## 10. UI 흐름
- **대기 화면**: "게임 시작" 버튼 + "조작법" 버튼(모달로 표시) + 최고 기록.
- **플레이 중 HUD**: 현재 점수, 수집한 로고 개수, 현재 시각
<!-- - **화면 오른쪽 세로 진행도 바**: 우리집(맨 아래) ~ 멀캠 도착(맨 위)까지 전체 거리를 세로 바 하나로
  표시. 바 위에 작은 캐릭터 아이콘이 현재 전진한 행 수 비율만큼 위로 움직이며 실시간 진행 상황을
  보여줌. 바 중간중간에 5개 스테이지 경계를 눈금/라벨로 표시해서 지금 어느 구간(큰길/버스정류장/
  지하철 승강장 등)을 지나는 중인지 한눈에 파악 가능. -->
- **결과 화면(성공/게임오버)**: 이번 판 기록 + 최고 기록 비교 + "다시 시작" 버튼.
  - 다시 시작 버튼을 빠르게 여러 번 연타해도 안전하게 한 번만 재시작되도록 방어 로직 적용.

## 11. 오디오
- 라이선스 이슈 없는 Web Audio API 오실레이터 기반 효과음 (이동/로고 획득/충돌/도착).
- 배경음악은 이번 버전 범위 밖.

## 12. 파일 구조
```
game/
  index.html
  style.css
  DESIGN.md
  js/
    main.js         # 부트스트랩 + 게임 루프
    scene.js        # Three.js scene/camera/lighting
    player.js       # 캐릭터 모델 + grid 이동
    input.js        # 방향키 입력 → hop 이벤트
    stages.js       # 5단계 스테이지 정의
    lanes.js        # 레인/타일 렌더링
    obstacles.js    # 차량/전동차 모델 + 이동 + 충돌 판정
    collectibles.js # AI 로고 아이템 + 파워업 효과 트리거/지속시간 관리
    clock.js        # 출근 시계 + 등급 판정
    save.js         # localStorage 최고 기록
    audio.js        # 효과음
    ui.js           # 상태 머신 + HUD + 재시작 방어 로직
  assets/
    logos/          # 사용자가 채워 넣을 실제 AI 로고 이미지
```

## 13. 남은 확인 사항
- 로고 세트는 **ChatGPT, Claude, Gemini, Copilot 4종**으로 확정.
- 로고 이미지 파일은 사용자가 직접 준비해서 `assets/logos/`에 넣어야 함.
- **AI 에이전트 마스코트 도우미**(위험 직전 신호를 주는 화면 속 캐릭터)는 이번 스코프에 포함하지
  않음 — 게임 1차 구현을 마친 뒤 추가할지 여부를 별도로 결정.
